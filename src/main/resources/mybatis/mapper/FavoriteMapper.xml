<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="subak.example.subak.dao.FavoriteDAO">
	<!-- 찜 중복 확인 -->
	<select id="checkFavoriteExists" parameterType="FavoriteDTO" resultType="int">
	    SELECT
	        COUNT(*)
	    FROM
	        favorite
	    WHERE
	        user_id = #{userId} AND item_id = #{itemId}
	</select>
	
	<insert id="insertFavorite" parameterType="FavoriteDTO">
		insert into favorite(created_at, user_id, item_id) values(sysdate, #{userId}, #{itemId})
	</insert>
	
	<delete id="deleteFavorite" parameterType="FavoriteDTO">
		delete from favorite
		where user_id = #{userId} and item_id = #{itemId}
	</delete>
</mapper>








